# JARVISFORME Render API

Environment variables:
- DATABASE_URL
- OPENAI_API_KEY
- OPENAI_MODEL (optional)
- JWT_SECRET
- FRONTEND_URL (optional)

The API listens on 0.0.0.0:$PORT and exposes /health, authentication, dashboard, study logging, backlog, AI chat, AI planner and mentor memory endpoints.
